# 72_占位锚点修复(1153条假锚定→1040真实锚定+113诚实降级)_执行记录_20260901

> 执行：老手 | 日期：2026-09-01 | 目的：最初更新目的"观点可下钻原文"的最后一公里
> 背景：70 号交接遗留"占位锚点 1487 修复"→ 本次落地（实际精确口径 1153 条 (0,0)）

## 一、侦察结论（先侦察再一步到位）

| 项 | 结论 |
|---|---|
| 占位锚点真实数量 | **1153 条**（anchor_start_ms=0 AND anchor_end_ms=0；此前记录 1320/1487 为不同口径估算） |
| 占位时间戳 | 全部 `00:00:00-00:00:00`（1145 条）或 `00:00-00:00`（8 条）——结构化提取时无时间信息 |
| 假锚定 | 这 1153 条却标 `anchor_confidence='high'`（00:00:00 被 timestamp_validator 判"合法"→anchored） |
| **关键发现** | raw_json.**evidence 字段藏着原文片段**（ASR 繁体原句，按 `(00:00:00-00:00:00)` 标记分隔）——提取器摘录了支撑句但没打时间 |
| 文本子串匹配 | claim 本身是提取器改写（非原文子串，命中 0），但 evidence 片段是原文（繁简归一后命中 90.2%） |

## 二、修复方案（确定性，零 LLM，不猜）

新脚本 `scripts/backfill_placeholder_anchors.py`（+ `scripts/zhcn_map.json` 繁简映射表）：

1. 只处理 (0,0) 占位 view（幂等，重跑不重复）
2. 从 evidence 提取括号标记后的原文片段（过滤逻辑概括段）
3. **繁→简归一**（本地 zhconv `getdict('zh-cn')` 导出 11906 条映射表，Mac 无 zhconv 的替代方案）+ 去空白标点
4. BV 内 transcript_segment 逐段子串匹配（长片段优先，6-16 字）
5. 命中 → anchor 更新为 segment 时间戳（confidence=medium, notes=text_fallback）+ 写 view_evidence_link
6. 未命中 → **诚实降级** unanchored + NULL + confidence=low（不再假锚定）

## 三、结果（Mac 权威库 ~/qianboshi_task）

| 指标 | 数值 |
|---|---|
| 命中锚定 | **1040 / 1153（90.2%）** |
| 诚实降级 | 113（无片段可锚） |
| 剩余占位 | **0** |
| 总 anchored | 6620 → **7660** |
| text_fallback links | 1040 |
| 抽验 | 5/5 片段↔segment 真实对应（含 ASR 原文错字"穿穿/八倍特段"——保真非错配） |
| 备份 | `qianboshi_quality.db.bak_20260901_132834_placeholder_fix` |

示例：`b6cf7387`（任泽平"AI的资本的开支天花板远远没有看到"）→ seg BV195MV6UE3B:921 @ 1667s-1669s ✅

## 四、联动与遗留

| 项 | 状态 |
|---|---|
| 恢复锚定的 1040 条中 788 条已在 claim 表 | ✅ 无需处理 |
| **252 条缺 view_evidence_annotation + claim**（tagger 全量时因 anchor=0 被排除） | ⏳ 待补：evidence_tagger 增量（gpt-5.6-sol 付费，~30-60min）→ 再 claim_backfill（免费）。**用户拍板后挂 Mac 后台** |
| 降级 113 条（87 条在 claim） | ✅ 渲染层"原文缺失"兜底已有，不影响 |

## 五、结论

占位锚点修复完成：**1153 条假锚定 → 1040 条真实可下钻 + 113 条诚实降级**。"观点可下钻原文"最后一公里打通。剩余 252 条补标注为可选后续（付费跑批，等拍板）。

## 六、联动收尾（同日）

| 项 | 结论 |
|---|---|
| **252 条缺标注补全** | ✅ 已挂 Mac 后台批跑（evidence_tagger `--view-ids` 白名单精确处理 252 条，gpt-5.6-sol；SSH 长连接托管，完成通知）。跑完接 claim_backfill（免费） |
| **evidence_tagger 版本漂移修复** | 🔧 本地 git 权威源码缺 `anchor_start_ms > 0`（8-31 修复只打了 Mac 没同步回本地）→ 本地补上 + 新增 `--view-ids` 参数（72 号精确补标注专用），scp 回 Mac |
| **#18 低置信显式标注+冲突标记** | ✅ **核对完成：已随 #5/#13/#66/#70 落地，无需新开发**——渲染实测：证据·置信列（🅰一手·✅核验·置信高/🅱二手·置信中）、预测力列（0.65/0.59/基准）、疑点·冲突列（🔀护盘·7多/4空、样本少、+N）、原文列+下钻详情全部就位。36 号蓝图标"P2 后置"系未核对代码现状 |

## 七、73 号计划（待批跑完成后）

1. claim_backfill 补 252 条 claim（确定性，免费）
2. 全量校验：占位 0 / anchored 7660+252 / annotation 7484+252 / claim 7484+252
3. 更新 33 蓝图核对表：#18 ✅（随 #5/#13/#66/#70）
