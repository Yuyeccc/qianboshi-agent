# 56_交接文档_20260831.md — qianboshi 认知内核升级会话交接（#17v2 + #14v2 双刀落地）

> 交接人：老手 | 日期：2026-08-31 深夜 | 上游：52 号交接 + 47/54 号方案 + 51/55 号执行记录
> 本文档自包含：接手者无需读历史对话即可开工。

---

## 一、本会话完成总览（两刀全闭环）

| 刀 | 状态 | 关键产出 | git |
|---|---|---|---|
| #17v2 纪律检查嵌入决策台 | ✅ | P0 回撤口径修正（cost_drawdown，盈利不再误报 61.5%）+ 决策台 discipline 段 + 模板契约 + output_gate 纪律硬阻断（红队 22/22）+ 16 测试 | 6299214 |
| #14v2 分层前瞻回测 | ✅ | tuple_status 五层互斥回测 + OOS + 四分母覆盖率 + 差值 CI；14 测试；全量 64 passed | e672629 |

## 二、架构增量（#14v2 数据流）

```
Mac quality.db（只读导出，不改库）
  claim.view_id → claim_reasoning_unit → reasoning_unit.tuple_status
  → data/view_tuple_status.json（7476 view：any_ok 聚合 + status_count，strict 对照差异=0）
  → scripts/layered_backtest_v2.py
      ├─ 五层 × 三方向(bull/bear/risk+all) × 5窗口 × 4段(all/train/test/roll)
      ├─ 事件级 + view 级计权命中率（分母=COUNT(DISTINCT view_id)）
      ├─ Wilson 95% CI + binomial 双侧5% + min_sample=30 + 差值CI(ok vs template_only)
      ├─ 四分母 resolved/error/skipped/eligible + 可评估覆盖率=resolved/(resolved+error)
      └─ → data/backtest/layered_report_*.json/.md + decision.db 新表
           prediction_backtest_layers（run_id 唯一键事务回滚）+ prediction_backtest_runs（meta）
```

## 三、关键数据结论（#14v2 首版，解读守边界）

- **覆盖率分层**：ok 70.6% vs partial_anchor 23.2% / template_only 39.6% / no_ru 22.6%
  → 完整推理链观点行情可评估性显著更高
- **OOS test 段命中率**：ok 全面优于 template_only（+7~13pp），**w3/w5 差值 CI 不含 0（显著）**；
  w20 双低（43.5%/36.3%）= 下跌市 20 天窗口看多贝塔
- **样本现实**：resolved 仅 728 view（全部 2026 年）；ok 127 / partial_anchor 49 / template_only 396 / no_ru 156
  → ok 层 train 段 n=20 <30 已标注样本不足
- template_only 层定位污染对照层，显著偏离 50% 不构成预测证据（gpt 意见 3 红线）

## 四、下一步队列（更新版）

1. **账本治理 P1**：history 缺 159992 买入 989.01 → 补流水/position ledger（#17v2 遗留）
2. **portfolio_max_cost 规则实现**（discipline_rules.json 已配置 10000）
3. **agent.py 简报链路接入 output_gate / brief_post_check**（#17v2 遗留：纪律契约目前靠模板）
4. **阈值校准**：0.70/0.50 支撑率阈值与 #14v2 分层命中率交叉验证
5. **8 月 error 事件行情补拉（backfill）→ 复跑对照**（#14v2 遗留，方案明确不混入首版）
6. 置换检验（可选）
7. 占位锚点 1487 修复 / 模板句条件补全 v2 / date 脏数据 56 条（既有队列）

## 五、红线（10 条，新增 #14v2 1 条）

1. unknown 不猜测，缺失标 ❔/待验证
2. 表达类型 ≠ 可信度等级
3. 冲突中心只并列原文不合并结论
4. 无来源数字不进首屏
5. 模板句确定性处理不调 LLM
6. 回测按 tuple_status 分层，**禁止混合**（总参考行标注"禁止引用"）
7. 只比对不输出调仓；简报禁买卖建议
8. Mac 跑批 DDL 禁并发；qianboshi_quality.db 跑批期间禁外部 DDL
9. 抽验必须逐条对照原文判定
10. **分层结论守解读边界**：template_only 显著偏离 50% ≠ 预测证据；差值 CI 含 0 不下显著结论；样本 <30 标注不足

## 六、环境手册（速查，同 52 号）

- Mac SSH：`SSH_ASKPASS="$HOME/askpass.sh" SSH_ASKPASS_REQUIRE=force DISPLAY=dummy` + `-o PubkeyAuthentication=no`；
  IP 192.168.1.25（mDNS 嗅探法：zeroconf 扫 _ssh._tcp.local.，用 C:/Python314/python.exe）
- Mac 权威库：`~/qianboshi_task/data/evidence/qianboshi_quality.db`（本地 quality.db 是空壳！）
- 本地 Python：C:/Python314/python.exe；unset PYTHONPATH
- 分层回测：`C:/Python314/python.exe scripts/layered_backtest_v2.py --report`（--dry-run 不写库）
- gpt 审核：codex exec（--skip-git-repo-check --sandbox read-only </dev/null）
- 挂件 todo：revision 216（剩余「完成数据库迁移方案」）

## 七、坑记录（新增 4 条，见 55 号执行记录第四节）

| 坑 | 修复 |
|---|---|
| 方案样本量假设偏差（resolved 全 2026 / 728 view） | 执行前自审 → C 节重写（walk-forward 3 段→1 段滚动） |
| run meta 未提交（事务外 insert） | 挪进 with con 事务块 |
| 报表格式（no_ru 重复行 / CI 重复） | 修复 |
| 挂件误标（名称含"回测"的无关任务） | 撤销恢复（改任务前先核对任务全名） |

## 八、验证清单（接手后先跑）

```bash
# 1. 全量回归（期望 64 passed）
cd /e/qianboshi-agent && C:/Python314/python.exe -m pytest tests/ -q
# 2. 分层回测可复现（期望新 run_id 写库，0 重复行）
C:/Python314/python.exe scripts/layered_backtest_v2.py --report --dry-run
# 3. 纪律检查实跑（期望 profit 不误报）
C:/Python314/python.exe scripts/discipline_checker.py --json | grep -E "drawdown_status|net_invested"
# 4. 合规红队（期望 22/22）
C:/Python314/python.exe scripts/compliance_gate.py --selfcheck
# 5. Mac 库完好（期望 annotation 7484 / claim 7484 / ru 10731）
SSH 后: cd ~/qianboshi_task && env -u PYTHONPATH python3 -c "import sqlite3;con=sqlite3.connect('data/evidence/qianboshi_quality.db');[print(t,con.execute(f'SELECT COUNT(*) FROM {t}').fetchone()[0]) for t in ('view_evidence_annotation','claim','reasoning_unit')]"
```

全过 = 环境完好，直接从第四节第 1 项开工。
