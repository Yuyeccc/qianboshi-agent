# 52_交接文档_20260831.md — qianboshi 认知内核升级会话交接（#17v2 纪律检查嵌入决策台 ✅ 落地）

> 交接人：老手 | 日期：2026-08-31 | 上游：50 号交接文档（v1，8-31 晚）+ 47 号方案（融合定稿）+ 51 号执行记录
> 本文档自包含：接手者无需读历史对话即可开工。

---

## 一、本会话完成总览（2026-08-31 深夜，单刀 #17v2）

| 模块 | 状态 | 关键产出 |
|---|---|---|
| 47 号方案拍板 | ✅ | 用户批准（侦察现状与方案 6 处全部吻合后拍板） |
| P0 回撤口径修正 | ✅ | cost_drawdown 成本口径，盈利组合不再误报 61.5%；实测 net=371.09 → -34.7% profit |
| decision_desk 嵌入 | ✅ | `_build_discipline()` 独立 try/except + `discipline` 段（失败只影响本段） |
| agent.py 注入 | ✅ | 第 4 段「决策台：组合纪律检查」全量注入不截断 |
| 模板 v3 纪律契约 | ✅ | 第 8 章追加 5 条契约（只陈述不处置/禁建议+动作/unknown 需人工确认） |
| output_gate 硬阻断 | ✅ | 纪律上下文+动作词共现 → block；红队 22/22（原 20 + 新 2） |
| 测试 + 回归 | ✅ | 16 passed（12 项方案全覆盖）；全量 50 passed |
| git 提交 | ✅ | 6299214（6 文件独立提交，可 revert 回滚） |

## 二、系统架构（数据流向 v6 + #17v2 纪律链路）

```
【Mac a1@192.168.1.25】权威数据层 ~/qianboshi_task/（本刀未触碰）
【Windows E:\qianboshi-agent】渲染/决策层
  data/portfolio.json（持仓权威账本：holdings + closed_positions；history 仅校验不重复计入）
  config/discipline_rules.json（三层纪律规则；portfolio_max_cost 已配置未实现）
  scripts/discipline_checker.py（#17v2：check_discipline → cost_drawdown 口径）
      ↓ 复用
  scripts/decision_desk.py（build_decision_desk_context 返回值 + discipline 段，独立 try/except）
      ↓ 注入
  scripts/agent.py morning_brief 链路（第 4 段「## 决策台：组合纪律检查」全量注入）
  templates/daily_brief_prompt_v3.md（第 8 章纪律契约约束 LLM 输出）
  scripts/compliance_gate.py（output_gate：DISCIPLINE_CONTEXT + ACTION 共现 → block，MCP 链路兜底）
```

**纪律输出 schema（固定）**：
```json
{checked_at, portfolio_updated_at, current_open_cost, gross_invested, recovered,
 net_invested, cost_drawdown, drawdown_status(profit/ok/breach/unknown/unverifiable),
 theme_ratios, violations, ok, data_quality}
```

## 三、文件清单（本刀新增/修改）

| 文件 | 作用 | 备注 |
|---|---|---|
| scripts/discipline_checker.py | #17v2 核心：cost_drawdown 口径 + _to_float 校验 + schema 固定 | git 6299214 |
| scripts/decision_desk.py | _build_discipline + discipline 段 | 同上 |
| scripts/agent.py | 第 4 段注入（全量不截断） | 同上 |
| templates/daily_brief_prompt_v3.md | 第 8 章纪律契约 | 同上 |
| scripts/compliance_gate.py | 纪律上下文 block 规则 + 红队 +2 | 同上 |
| tests/test_discipline_17v2.py | 16 项测试（方案 12 项全覆盖） | 同上 |
| data/webpage_design/51_#17v2纪律检查嵌入决策台_执行记录_20260831.md | 本刀执行记录 | 含坑 2 条 |

## 四、下一步队列（更新版）

1. **#14v2 真回测**（P1，新排第一）：按 tuple_status（ok/template_only/partial_anchor）**分层统计禁止混合**；需 prediction_events + 结果窗口 + OOS 切分；现有 layered_backtest 56 行是支撑率基线 v0 非前瞻回测
2. **账本治理 P1**（#17v2 遗留）：history 缺 159992 买入 989.01 → 补流水 / position ledger；补齐后纪律检查 net_invested 更精确
3. **portfolio_max_cost 规则实现**（配置已存在 10000，规则逻辑未写）
4. **agent.py 简报链路接入 output_gate / brief_post_check**（#17v2 遗留：纪律契约目前靠模板约束，LLM 链路无硬阻断）
5. 阈值校准（0.70/0.50 待 #14v2 OOS 数据）
6. 占位锚点 1487 修复（partial_anchor 升级完整五元组前置）
7. 模板句条件补全 v2 + 8 条 LLM 未返回 claim（needs_review）
8. date 脏数据 56 条（2016×39 + 2023×17，view_extractor 历史抓错）

## 五、红线（9 条，同 50 号）

1. unknown 不猜测，缺失标 `❔原文缺失`/`待验证`
2. 表达类型（fact/interpretation/forecast）≠ 可信度等级，渲染并列 source_layer/verification_status
3. 冲突中心只并列原文，不合并结论
4. 无来源数字不进首屏（evidence_gate，--no-evidence-gate 可关）
5. 模板句确定性处理不调 LLM，五元组留 NULL 不臆造
6. 回测按 tuple_status 分层，禁止混合
7. 只比对不输出调仓（纪律检查）；简报 LLM 链路禁买卖建议（模板契约 + output_gate 兜底）
8. Mac 跑批 DDL 禁并发；qianboshi_quality.db 跑批期间禁外部 DDL
9. 抽验必须逐条对照原文判定；LLM 结论串位风险 → ru_tagger 自检 + 人工抽验双保险

## 六、环境手册（同 50 号，速查）

- **Mac SSH 铁律**：`export SSH_ASKPASS="$HOME/askpass.sh" SSH_ASKPASS_REQUIRE=force DISPLAY=dummy` + `-o PubkeyAuthentication=no`；密码四空格；IP 漂移用 mDNS（a1.local）
- **跑批铁律**：multiprocessing 勿 threading（SIGSEGV）；nohup 被 SSH 断开杀 → 本地 SSH 长连接托管；脚本 scp 执行勿走 heredoc
- **本地 Python**：C:/Python314/python.exe（PYTHON_EXE）；unset PYTHONPATH（被劫持）
- **LLM**：Mac 用 fluxionai premium stream；本地 gpt-5.6-sol 用 codex exec 直连（勿走 call_premium，`</dev/null` 防挂）
- **挂件 todo**：revision 215，#17v2 已勾；剩余「完成数据库迁移方案」

## 七、坑记录（本刀新增 2 条）

| 坑 | 现象 → 修复 |
|---|---|
| 缺 proceeds 仍硬判 | 部分卖出缺 proceeds 时 net 仍>0 算出 breach → 改 unverifiable 状态不硬判（gpt 意见 4） |
| 测试数据构造失误 | p_ok 半导体 80% 本身超限、黄金 30% 超 20% cap → 改边界断言（恰好==cap 不触发） |

（50 号既有 5 条坑仍有效：抽验清单是摆设 / LLM 结论串位 / 词面重合率误拦 / fix2 B 类空 claim / nohup 后台被杀）

## 八、验证清单（接手后先跑确认环境完好）

```bash
# 1. 纪律检查实跑（期望：profit 不误报 + 真实主题/单标的违规照常）
cd /e/qianboshi-agent && C:/Python314/python.exe scripts/discipline_checker.py --json
# 2. 决策台注入自检（期望：discipline.ok=False / cost_drawdown=-0.3474 / 无 error）
C:/Python314/python.exe -c "import sys; sys.path.insert(0,'scripts'); from decision_desk import build_decision_desk_context; d=build_decision_desk_context(); print(d['discipline']['ok'], d['discipline']['cost_drawdown'], d['discipline']['drawdown_status'])"
# 3. 新测试 + 全量回归（期望 16 + 50 passed）
C:/Python314/python.exe -m pytest tests/test_discipline_17v2.py -q && C:/Python314/python.exe -m pytest tests/ -q
# 4. 合规红队（期望 22/22）
C:/Python314/python.exe scripts/compliance_gate.py --selfcheck
# 5. Mac 库完好（期望 annotation 7484 / claim 7484 / ru 覆盖 7476+）
export SSH_ASKPASS="$HOME/askpass.sh" SSH_ASKPASS_REQUIRE=force DISPLAY=dummy
ssh -o PubkeyAuthentication=no a1@192.168.1.25 'cd ~/qianboshi_task && env -u PYTHONPATH python3 ru_verify.py 2>&1 | head -14'
```

全过 = 环境完好，直接从第四节第 1 项（#14v2 真回测）开工。
