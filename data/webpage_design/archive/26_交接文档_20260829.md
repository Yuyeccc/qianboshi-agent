# 交接文档 — 2026-08-29 钱博士 Agent 工作日报与交接

> 生成：老手 | 日期：2026-08-29（周六）
> 目的：固化今日全部工作成果、踩坑记录、待办事项，供下次会话无缝继续

---

## 一、今日完成事项总览

### 2. 队列消化 + 观点补全（已完成 ✅）
- **队列消化**：21 条积压全部处理完毕（含 8/27-8/28 最新直播：旗帜鲜明/钱博士BRTV/李一恩/史诗级韭菜/趋势天哥）
  - 转写 → 纠错 → 结构化 → RAG 全部完成，队列清零
  - 新增笔记 21 篇，笔记总数 1052
- **观点补全**：view_extractor --since 2026-08-27 两轮抽取，观点库 11192 → **11312**（8/27+ 共 181 条）
- ⚠️ **严重坑（已修）**：drain 转写期间 bv_pubdates.json 还是字符串日期格式，
  `datetime.fromtimestamp("2026-08-28")` 抛 TypeError 被 catch 吞掉 → 笔记 frontmatter date
  保留了 LLM 猜的假日期（2024/2025）！修复：`C:\tmp\fix_note_dates.py`（从**文件名**提取 BV，
  不在 frontmatter！）+ 补拉 5 个深研一点 BV 的 pubdate（BV182hw6BEh8/BV1CW4o6TEMV/BV1Drtw6cE2e/
  BV1ks4o6iEbW/BV1S54o6VEvs）→ 15 篇笔记日期全部修正 → 重跑 view_extractor 补观点
- **磁盘清理**：audio/ 半年外删除 160 个文件，释放 54.7G（E盘 93%→90% 占用）
- **pubdate 修复**：15 条无日期视频用 B站 API 补全（史诗级韭菜 8/2-8/28 全补齐）
  - ⚠️ **坑**：bv_pubdates.json 的 pubdate 必须是**时间戳 int**，不是 "YYYY-MM-DD" 字符串！
    `datetime.fromtimestamp()` 直接崩 → 已写脚本修正 15 条。脚本在 `C:\tmp\fix_pubdate_format.py`
- **新脚本**：`E:\qianboshi-agent\scripts\fix_queue_pubdates.py`（队列补日期，注意格式陷阱）

### 2. DSH（DeepSeek Harness）接入（已完成 ✅ 全链路验证）
- **背景**：DeepSeek 官方 8-13 开源 agent 框架，20.2 万 star，MIT，TypeScript
- **安装**：`@deepseek-ai/dsh@0.1.1-rc.2`（npm 全局）+ pnpm 11.24.0
- **qianboshi 专用 profile**：`C:\Users\1\.dsh\profiles\qianboshi\`
  - `cordis.patch.yml`：dsh-mcp-client 直连 `C:/Python314/python.exe E:/qianboshi-agent/scripts/qianboshi_mcp.py`
  - 13 个 MCP 工具全部注册成功（`mcp__qianboshi__*`）
- **凭据**：`C:\Users\1\.dsh\.credentials.yaml`（ref = `DEEPSEEK_API_KEY`，官方 key 35 字符）
- **工作区**：`C:\Users\1\.dsh\storages\workspace.json`（ws-qianboshi → E:\qianboshi-agent）
- **验证**：
  - headless：`dsh --profile headless "..."` 调用 get_asset_analysis_card(GOLD) ✅
  - Web UI：http://127.0.0.1:3080 调 get_debate_card(半导体) ✅ 输出三类分歧+共识
- **启动命令**：`cd ~/.dsh/profiles/qianboshi && dsh --profile qianboshi --no-open --port 3080`
  - ⚠️ 注意：`--profile` 是 parent flag，**不能**跟 `web` 子命令混用（会报错）
- **已装插件**：dshmarket（插件市场）/ dsh-context（上下文可视化）/ dsh-quota（余额 ¥9.68 胶囊）
- **踩坑记录**（简历素材）：
  1. patch.yml 新增插件必须用 `insert:` 语法（不能直接列条目）
  2. profile 混用 npm+pnpm 导致 dsh-tools 版本分裂（0.0.1-rc.1 vs 0.1.1-rc.2 scheduler 崩）→ 清 node_modules 只用 pnpm
  3. 工作区必须先写 storage registry 才能选
  4. 端口 3080 残留进程要 `powershell Stop-Process` 强杀

### 3. 竞品/开源调研（已完成 📚）
- **OpenBiliClaw** ⭐3091（`E:\OpenBiliClaw`）：B站等跨平台内容发现 Agent
  - 4 种发现策略：search/trending/related_chain/explore（防回音室设计）
  - 值得借鉴：搜索预算控制、探索域饱和检测、画像驱动发现
- **Vibe-Research** ⭐2207（`E:\Vibe-Research`）：**最接近我们需求的样板**（基于 Codex Harness 的金融研究工作台）
  - 六阶段研究 SOP + 阶段 validator + 确定性计算库 calc/ + 合规 gate + 数据端点注册表（116 端点×市场×合规分级）
  - 金融研究宪法 AGENTS.md（三条不可越线 + 数据纪律五问 Gate）
  - **只深研 A 股，无分析师直播观点/无用户逻辑学习** = 我们的差异化空间
- **Vibe-Trading** ⭐32k（HKUDS）：记忆 Tier2 结构化 + 质量评分 + Ebbinghaus 衰减 + 归档 GC
- **dsh-openbiliclaw**（`E:\dsh-openbiliclaw`）：DSH 插件范本（22 工具，requestId 幂等设计）

### 4. 投研工作台架构设计（已完成 ✅ 定稿）
- **需求说明书**：`data/webpage_design/20_工作台需求说明书.md`
- **架构文档**（编号连续，在 `E:\qianboshi-agent\data\webpage_design\`）：
  - `21_工作台架构_gpt版.md` / `21_工作台架构_老手版.md`（v1 双版本）
  - `22_工作台架构_融合终版.md`（v1 融合）
  - `23_独立Agent架构_gpt商量版.md`（独立灵魂讨论）
  - `24_工作台架构v3_gpt版.md` / `24_工作台架构v3_老手版.md`（v3 双版本）
  - **`25_工作台架构v3_融合终版.md`（定稿，看这个就行）**
- **核心决策**：
  - 方向：**qianboshi 独立于 Hermes**（Hermes 降级为可选适配器，qbs CLI 可独立运行）
  - 灵魂 = 财经认知内核（原始证据+条件化推理+确定性计算+可验证前提+可复盘记忆）
  - 差异化护城河：分析师直播推理链保真 + 多源前提校验 + 分析师逻辑沉淀复用 + 用户决策成长 + A/港/美分域
  - 五对象保真模型：raw_asset → transcript_segment → reasoning_unit → claim → research_output
  - 执行层三件套：calc 确定性计算 + 阶段 validator + 合规 gate（借鉴 Vibe-Research）

---

## 二、下一步（按优先级）

### P0：Phase 0 数据底座止损（1-2 周）
1. 核查 ASR 是否保留完整 JSON（当前可能只存文本？）→ 补全
2. 写 `evidence_extractor.py`：ASR 切语义段 → reasoning_unit 入库（参考 Vibe transcript_segment 字段）
3. 现有笔记/观点表加 evidence_ids + 历史回填迁移脚本
4. 金融研究宪法 AGENTS.md 初版（参考 `E:\Vibe-Research\AGENTS.md`）
5. qbs CLI 骨架（ingest/process-events/research/morning-brief/notify）
6. ✅ 验收：任一日报观点可下钻到直播原文时间点

### P1：Phase 1 单人工作台（2-3 周）
- FastAPI + React 骨架 + JWT（参考作品集站技术栈）
- 今日工作台/资产卡/多空卡/证据抽屉/对话 SSE/工具轨迹

### P2：内测多用户 + 决策成长（后续）
- 邀请码/配额/workspace 隔离/持仓联动/决策复盘

---

## 三、关键文件索引

| 用途 | 路径 |
|---|---|
| 架构定稿 | `E:\qianboshi-agent\data\webpage_design\25_工作台架构v3_融合终版.md` |
| 需求说明书 | `E:\qianboshi-agent\data\webpage_design\20_工作台需求说明书.md` |
| DSH profile | `C:\Users\1\.dsh\profiles\qianboshi\` |
| DSH 凭据 | `C:\Users\1\.dsh\.credentials.yaml` |
| 工作区注册 | `C:\Users\1\.dsh\storages\workspace.json` |
| Vibe-Research 源码 | `E:\Vibe-Research\`（重点：AGENTS.md / calc/ / orchestrator/src/gate.ts / datasources/registry.json） |
| OpenBiliClaw 源码 | `E:\OpenBiliClaw\` |
| DSH 插件范本 | `E:\dsh-openbiliclaw\` |
| 队列日期修复 | `E:\qianboshi-agent\scripts\fix_queue_pubdates.py` |

---

## 四、环境备忘（重要！）

- **Python 子进程**：必须 `env -u PYTHONPATH /c/Python314/python.exe`（Hermes 会劫持 python 到 hermes-web-ui venv，无 yt_dlp；PYTHONPATH 会污染）
- **DSH 启动**：`cd ~/.dsh/profiles/qianboshi && dsh --profile qianboshi --no-open --port 3080`（后台）
- **Web UI**：http://127.0.0.1:3080（内测声明点"继续"，工作区选 qianboshi-agent）
- **fluxionai gpt-5.6-sol**：key 在 `C:/tmp/fluxionai_key.txt`，必须 stream=False，max_tokens ≤3500，走 `llm_fallback.call_premium()`
- **模型分工**：routine=deepseek-v4-flash，analysis=premium(sol) fallback flash；禁 v4-pro
- **飞书**：NO_PROXY=open.feishu.cn 直连（Clash 代理 127.0.0.1:7897 挂掉时飞书会失败）

---

## 五、今日踩坑速查（避免重踩）

1. bv_pubdates.json pubdate = **时间戳 int**，不是字符串
2. DSH patch.yml 加插件用 `insert:`；profile 只用 pnpm 别混 npm
3. DSH `--profile` 不能与 `web` 子命令同用
4. 3080 端口残留：`powershell -Command "Stop-Process -Id <pid> -Force"`
5. read_file 遇到 CRLF 文件会误判 binary → 先转 LF 再读
6. wmic 查进程用 `wmic process where "name='python.exe'" get ...`（有中文乱码但能用）
7. 长任务（转写/LLM）一律后台 + notify_on_complete，别前台等

---

## 六、给下次自己的话

架构已定稿（v3），方向明确：**让 qianboshi 拥有自己的灵魂**（认知内核），Hermes 只当执行器。
Phase 0 第一刀：核查 ASR JSON 保真度 → evidence_extractor。开工前先读 `25_工作台架构v3_融合终版.md` 和本交接文档。
