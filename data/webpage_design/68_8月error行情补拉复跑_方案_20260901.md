# 68_8月error行情补拉复跑_方案_20260901.md

> 生成：老手（侦察量化先行）| 日期：2026-09-01 | 上游：67 号执行记录遗留第 1 项
> 状态：**待用户拍板** → 执行 → 验证

---

## 一、目标

补拉 2026 年 error 事件的缺失历史行情，重跑 resolve + #14v2 复跑对照，
扩大回测覆盖（可评估率），为阈值查表再校准提供更大样本。

## 二、现状（侦察量化）

- error 事件 12666 = 2016-2025 老事件（归档，不补）+ **2026 年 2956 事件（可补）**
- **2026 error 原因量化**：p0 历史缺失 **2515（85%）** / p1 断档 27 / 其他 414
  （p0 缺失 = 1-5 月事件早于 price_trends 行情起始 6 月中）
- 涉及 7 symbols：300502.SZ 815 / 300308.SZ 600 / 688981.SS 521 / 300394.SZ 450 / 601899.SS 449 / 159992.SZ 70 / 159770.SZ 51
- yfinance 实测可用（688981.SS 拉到 2025-09 起 242 行，够覆盖 2026-01 p0）
- error 事件不在 outcome_updater 的 pending 集合 → 需先重置状态

## 三、方案

### A. scripts/backfill_history_prices.py（新脚本）
- 7 symbols × yfinance `history(period='1y', auto_adjust=True)`（dropna 末尾 nan）
- **增量合并**写 price_trends.json（保留现有 184 symbols 数据，不覆盖；冲突以新为准）
- 逐标的 sleep(1.0) 防限流；失败标的报错不中断
- 备份 price_trends.json → .bak_20260901_pre_backfill

### B. 重置 + 重跑 resolve
```bash
# 重置 2026 error → pending（可回滚：UPDATE 反向）
UPDATE prediction_events SET status='pending' WHERE status='error' AND event_date >= '2026-01-01'
C:/Python314/python.exe scripts/outcome_updater.py --limit 20000 --since 2026-01-01
```

### C. #14v2 复跑对照
- `layered_backtest_v2.py --report`（新 run_id，旧 run 保留）
- 对比：可评估覆盖率（resolved/(resolved+error)）/ 各层命中率 / 样本量

### D. 阈值查表再校准（样本扩大后）
- `threshold_calibration.py --report` → 与 65 号对比，查表值若有实质变化 → 更新 prediction_confidence.TABLE

### E. 测试（tests/test_backfill_prices.py）
1. 合并逻辑：新数据增量、已有 symbol 保留不覆盖（构造 tmp json）
2. 失败标的容错（mock yfinance 抛错 → 不中断）
3. 只读约束：除 price_trends.json 外数据文件 hash 不变

## 四、影响面与回滚

- 改 2 数据文件（price_trends.json 增量 + prediction_events 状态重置）+ 1 脚本 + 1 测试
- 回滚：price_trends 从 .bak 恢复；prediction_events UPDATE 反向；#14v2 旧 run 仍在
- 不触碰 Mac / claim / reasoning_unit / annotation

## 五、验证清单（交付门）

```bash
# 1. 补行情（期望 7 symbols 覆盖到 2025-09 前）
C:/Python314/python.exe scripts/backfill_history_prices.py
# 2. 重跑 resolve（期望 error 大幅下降，resolved 增加 ~2500+）
C:/Python314/python.exe scripts/outcome_updater.py --limit 20000 --since 2026-01-01
# 3. #14v2 复跑（期望覆盖率提升，命中率口径一致可对比）
C:/Python314/python.exe scripts/layered_backtest_v2.py --report
# 4. 全量回归（期望 93 + N passed）
C:/Python314/python.exe -m pytest tests/ -q
```
