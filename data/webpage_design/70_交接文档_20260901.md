# 70_交接文档_20260901.md — qianboshi 认知内核升级会话交接（7 刀全落地，主线队列清零）

> 交接人：老手 | 日期：2026-09-01 | 上游：59 号交接 + 54/57/60/62/64/66/68 号方案 + 55/58/61/63/65/67/69 号执行记录
> 本文档自包含：接手者无需读历史对话即可开工。

---

## 一、本会话完成总览（7 刀全闭环）

| # | 刀 | 状态 | 关键产出 | git |
|---|---|---|---|---|
| 1 | #17v2 纪律检查嵌入决策台 | ✅ | cost_drawdown 口径（盈利不再误报 61.5%）+ discipline 段 + 模板契约 + output_gate 纪律硬阻断（红队 22/22） | 6299214 |
| 2 | #14v2 分层前瞻回测 | ✅ | tuple_status 五层互斥 + OOS（主切分+滚动）+ 四分母覆盖率 + 差值 CI；ok 层 OOS 优于模板层 w3/w5 显著 | e672629 |
| 3 | 账本治理 P1 | ✅ | history 补 159992 买入 989.01（账本闭合 2289.01/1917.92）+ ledger_verify 5 项校验 | a7f6fa7 |
| 4 | portfolio_max_cost 第 4 层纪律 | ✅ | 持仓总成本上限规则（>触发/边界相等不触发/配置异常跳过） | b07e69d |
| 5 | agent 简报链路 output_gate | ✅ | _gate_brief（clean/annotate/block+fail-closed），三分支接入 | 89fb041 |
| 6 | 阈值校准（双审） | ✅ | authority 实证矩阵（view 级聚类+窗口敏感性）；**证据权威≠预测能力**（43-52% 全≈随机）；语义解耦定案 | d72d283 |
| 7 | prediction_confidence 消费层 | ✅ | 实证查表（authority×tuple_status）+ RAG 排序校正 + load_views 注入（7476/10702） | 5811cce |
| 8 | 8月 error 行情补拉复跑 | ✅ | 7 symbols×1y 增量合并；resolved 6984→**9935（+42%）**；#14v2 结论稳健；查表无需更新 | 2a7c3d0 |

全量回归：**96 passed** | 合规红队：22/22 | Mac 库未触碰（仅只读导出 3 次）

## 二、架构现状（数据流）

```
【Mac a1@192.168.1.25】~/qianboshi_task/data/evidence/qianboshi_quality.db（权威，勿改）
  view_raw 11311 / annotation 7484 / claim 7484 / reasoning_unit 10731
【Windows E:\qianboshi-agent】渲染/决策层
  data/portfolio.json（账本闭合：holdings 1 + closed 2 + history 9）
  data/price_trends.json（184 symbols；7 个补深到 2025-09 前）
  data/view_authority.json（7484 view→authority）+ data/view_tuple_status.json（7476 view→layer）
  scripts/discipline_checker.py（4 层纪律：主题/单标的/cost_drawdown/portfolio_max_cost）
  scripts/decision_desk.py → agent.py（第 4 段纪律 + _gate_brief 合规门）
  scripts/layered_backtest_v2.py（分层回测，resolved 9935）
  scripts/threshold_calibration.py（authority 实证矩阵）
  scripts/prediction_confidence.py（消费层查表，view_store 排序注入）
  scripts/ledger_verify.py（账本 5 项校验）+ scripts/backfill_history_prices.py（行情补拉）
  scripts/compliance_gate.py（output_gate：MCP + 简报双硬阻断）
```

## 三、关键数据结论（可引用）

1. **#14v2 分层回测**（补行情后）：ok 层 OOS 全面优于 template_only（test 段 w3 +12.1pp / w5 +11.6pp 显著）；
   覆盖率 ok 78.8% > template_only 61.1% > no_ru 32.6% ≈ partial 31.3%
2. **阈值校准**（view 级独立样本 982 view）：authority 对预测命中率无区分度（43-53% 全≈随机）；
   conf3 映射（A0.9/B0.7/C0.5/D0.3）= 证据可信度语义，**禁止当预测概率用**；预测力走 prediction_confidence（0.49-0.65）
3. **纪律检查**（#17v2+#17v3）：net_invested=371.09 / profit / 真实违规=主题 100%>30% + 单标的 100%>30%
4. **账本**：买入 2289.01=gross_invested / 卖出 1917.92=recovered，ledger_verify 全过

## 四、下一步队列（全部 P2 级，主线清零）

1. brief_renderer 显示层预测力徽标（P2，可选增强：置信列旁加"预测力"标注）
2. position ledger 完整账本模型（P2，待消费方：收益归因/税务/多账户）
3. 置换检验（P2，可选：#14v2 显著性补充）
4. 2016-2025 老 error 事件归档（9715 剩余，8-06 定案不补）
5. 既有长尾：占位锚点 1487 修复 / 模板句条件补全 v2 / date 脏数据 56 条
6. 数据库迁移方案（挂件 todo 唯一未勾项）

## 五、红线（12 条）

1-11 同 59 号（unknown 不猜测 / 表达类型≠可信度 / 冲突只并列 / 无来源数字不进首屏 / 模板句不调 LLM / 回测分层禁混合 / 只比对不输出调仓 / Mac DDL 禁并发 / 抽验逐条对照 / 分层结论守解读边界 / 账本改动过 ledger_verify）
12. **证据分数与预测分数解耦**：conf3 映射是证据可信度，预测力只能走 prediction_confidence；显示/排序不得混用

## 六、环境手册（速查）

- Mac SSH：`SSH_ASKPASS="$HOME/askpass.sh" SSH_ASKPASS_REQUIRE=force DISPLAY=dummy` + `-o PubkeyAuthentication=no`；IP 192.168.1.25（漂移用 zeroconf 扫 _ssh._tcp.local.，C:/Python314/python.exe）
- Mac 权威库：`~/qianboshi_task/data/evidence/qianboshi_quality.db`（本地 quality.db 是空壳！）
- 本地 Python：C:/Python314/python.exe；unset PYTHONPATH
- 常用命令：
  - 纪律：`C:/Python314/python.exe scripts/discipline_checker.py --json`
  - 回测：`C:/Python314/python.exe scripts/layered_backtest_v2.py --report`（--dry-run 不写库）
  - 校准：`C:/Python314/python.exe scripts/threshold_calibration.py --report`
  - 账本：`C:/Python314/python.exe scripts/ledger_verify.py`
  - 行情补拉：`C:/Python314/python.exe scripts/backfill_history_prices.py`
- gpt 审核：codex exec（--skip-git-repo-check --sandbox read-only </dev/null）
- 挂件 todo：revision 216（剩余「完成数据库迁移方案」）

## 七、坑记录（本会话累计 9 条，新增见各执行记录）

| 坑 | 修复 |
|---|---|
| 缺 proceeds 仍硬判 | unverifiable 状态不硬判（#17v2） |
| 方案样本量假设偏差 | 执行前自审 → C 节重写（#14v2） |
| run meta 未提交 | 挪进事务块（#14v2） |
| 报表格式/测试占位断言 | 修复（#14v2） |
| 负数仓位误报 | 份额对账（账本） |
| 测试配置缺失 | RULES 补 portfolio_max_cost（#17v3） |
| patch 目标错误 | 打 compliance_gate.output_gate（agent gate） |
| 防重复测试构造错 | 已含横幅文本断言 count==1（agent gate） |
| 窗口口径偏差 | view 级聚类 + 敏感性（阈值校准，gpt 意见 2） |

## 八、验证清单（接手后先跑）

```bash
# 1. 全量回归（期望 96 passed）
cd /e/qianboshi-agent && C:/Python314/python.exe -m pytest tests/ -q
# 2. 纪律检查（期望 profit / net_invested=371.09）
C:/Python314/python.exe scripts/discipline_checker.py --json | grep -E "drawdown_status|net_invested"
# 3. 账本校验（期望 ✅ 通过）
C:/Python314/python.exe scripts/ledger_verify.py
# 4. 红队（期望 22/22）
C:/Python314/python.exe scripts/compliance_gate.py --selfcheck
# 5. 回测状态（期望 resolved 9935 / error 9715）
C:/Python314/python.exe -c "import sqlite3;c=sqlite3.connect('data/qianboshi_decision.db');print(c.execute(\"SELECT status,COUNT(*) FROM prediction_events GROUP BY status\").fetchall())"
# 6. Mac 库完好（7484/7484/10731）
SSH 后: cd ~/qianboshi_task && env -u PYTHONPATH python3 -c "import sqlite3;con=sqlite3.connect('data/evidence/qianboshi_quality.db');[print(t,con.execute(f'SELECT COUNT(*) FROM {t}').fetchone()[0]) for t in ('view_evidence_annotation','claim','reasoning_unit')]"
```

全过 = 环境完好。主线队列已清零，剩余 P2 级项目按需推进。
