# 59_交接文档_20260901.md — qianboshi 认知内核升级会话交接（三刀落地：#17v2 + #14v2 + 账本治理P1）

> 交接人：老手 | 日期：2026-09-01 凌晨 | 上游：56 号交接 + 47/54/57 号方案 + 51/55/58 号执行记录
> 本文档自包含：接手者无需读历史对话即可开工。

---

## 一、本会话完成总览（三刀全闭环）

| 刀 | 状态 | 关键产出 | git |
|---|---|---|---|
| #17v2 纪律检查嵌入决策台 | ✅ | cost_drawdown 口径修正（盈利不再误报 61.5%）+ decision_desk discipline 段 + 模板契约 + output_gate 纪律硬阻断（红队 22/22） | 6299214 |
| #14v2 分层前瞻回测 | ✅ | tuple_status 五层互斥回测 + OOS（主切分+滚动）+ 四分母覆盖率 + 差值 CI；ok 层 OOS 优于模板层 w3/w5 显著 | e672629 |
| 账本治理 P1 | ✅ | history 补 159992 买入 989.01（账本闭合 2289.01/1917.92）+ ledger_verify 5 项校验 + 8 测试 | a7f6fa7 |

全量回归：**72 passed** | 合规红队：22/22 | Mac 库未触碰（仅只读导出）

## 二、架构现状（数据流）

```
【Mac a1@192.168.1.25】~/qianboshi_task/data/evidence/qianboshi_quality.db（权威，勿改）
  view_raw 11311 / view_evidence_annotation 7484 / claim 7484 /
  reasoning_unit 10731（tuple_status: ok 5487 / partial_anchor 1487 / template_only 3757）
【Windows E:\qianboshi-agent】渲染/决策层
  data/portfolio.json（账本已闭合：holdings 1 + closed 2 + history 9 条）
  data/view_tuple_status.json（7476 view→layer 映射，any_ok 聚合）
  scripts/discipline_checker.py（#17v2 cost_drawdown）→ decision_desk.py discipline 段 → agent.py 注入
  scripts/layered_backtest_v2.py（#14v2 分层回测）→ data/backtest/layered_report_*.{json,md}
  scripts/ledger_verify.py（账本 5 项一致性校验）
  scripts/compliance_gate.py（output_gate 纪律硬阻断）
```

## 三、下一步队列（更新版）

1. **阈值校准**：0.70/0.50 支撑率阈值与 #14v2 分层命中率交叉验证
2. **8 月 error 事件行情补拉（backfill）→ #14v2 复跑对照**
3. position ledger 完整账本模型（P2，待消费方）
4. 置换检验（可选）
5. 占位锚点 1487 修复 / 模板句条件补全 v2 / date 脏数据 56 条（既有队列）

> 注：portfolio_max_cost 第 4 层纪律 ✅（git b07e69d，61 号记录）；agent 简报链路 output_gate ✅（git 89fb041，63 号记录）

## 四、红线（11 条，新增账本 1 条）

1-10 同 56 号（unknown 不猜测 / 表达类型≠可信度 / 冲突只并列 / 无来源数字不进首屏 / 模板句不调 LLM / 回测分层禁混合 / 只比对不输出调仓 / Mac 跑批 DDL 禁并发 / 抽验逐条对照 / 分层结论守解读边界）
11. **账本改动用校验门**：portfolio.json 任何改动必须过 `ledger_verify.py`（买入=Σclosed+holdings / 卖出=Σproceeds / 份额不超卖）；金额卖出>买入=盈利正常，负数仓位按份额判定

## 五、环境手册（速查）

- Mac SSH：`SSH_ASKPASS="$HOME/askpass.sh" SSH_ASKPASS_REQUIRE=force DISPLAY=dummy` + `-o PubkeyAuthentication=no`；IP 192.168.1.25（漂移用 zeroconf 扫 _ssh._tcp.local.）
- 本地 Python：C:/Python314/python.exe；unset PYTHONPATH
- 纪律：`C:/Python314/python.exe scripts/discipline_checker.py --json`
- 回测：`C:/Python314/python.exe scripts/layered_backtest_v2.py --report`（--dry-run 不写库）
- 账本：`C:/Python314/python.exe scripts/ledger_verify.py`（退出码 0=通过）
- gpt 审核：codex exec（--skip-git-repo-check --sandbox read-only </dev/null）
- 挂件 todo：revision 216（剩余「完成数据库迁移方案」）

## 六、验证清单（接手后先跑）

```bash
# 1. 全量回归（期望 72 passed）
cd /e/qianboshi-agent && C:/Python314/python.exe -m pytest tests/ -q
# 2. 账本校验（期望 ✅ 通过）
C:/Python314/python.exe scripts/ledger_verify.py
# 3. 纪律检查（期望 profit / net_invested=371.09）
C:/Python314/python.exe scripts/discipline_checker.py --json | grep -E "drawdown_status|net_invested"
# 4. 红队（期望 22/22）
C:/Python314/python.exe scripts/compliance_gate.py --selfcheck
# 5. 分层回测可复现（新 run_id 0 重复）
C:/Python314/python.exe scripts/layered_backtest_v2.py --report --dry-run
# 6. Mac 库完好（7484/7484/10731）
```

全过 = 环境完好，从第四节第 1 项（portfolio_max_cost）开工。
