# 73_交接文档_20260901.md — 模型切换(deepseek→GLM) + 认知内核收尾交接

> 交接人：老手 | 日期：2026-09-01 | 原因：**deepseek 即将升高峰价格 → 用户切换 GLM**；本轮认知内核升级收尾
> 本文档自包含：接手者（GLM 会话）无需读历史对话即可开工。

---

## 一、模型切换（本次核心变更，已验证）

### 1. Hermes 会话层（用户已切，9-01）
- default: `glm-5.3-flash`，provider `zai`（base_url `https://open.bigmodel.cn/api/paas/v4`）
- fallback: `deepseek-v4-flash`
- 凭据：Hermes zai provider 读 `GLM_API_KEY / ZAI_API_KEY / Z_AI_API_KEY`（**注意：不读 ZHIPU_API_KEY**）

### 2. qianboshi 业务层（本次由老手切换 + 冒烟验证 ✅）
`E:\qianboshi-agent\config.yaml`（备份 `config.yaml.bak.20260901_pre_glm`）：
```yaml
llm:
  api_base: https://open.bigmodel.cn/api/paas/v4   # 原 api.deepseek.com/v1
  api_key_env: ZAI_API_KEY                          # 原 DEEPSEEK_API_KEY（.env 里两个 key 都在）
  routine_model: glm-5.3-flash                      # 原 deepseek-v4-flash
  analysis_model: glm-5.3-flash                     # 原 deepseek-v4-flash
  max_tokens: 8192                                  # 保持
```
- **premium（gpt-5.6-sol，fluxionai）不受影响**（分析/简报/教训类仍优先 sol）
- 冒烟实测：`GLM切换成功` ✅（max_tokens=8192 时）
- ⚠️ **GLM-5.3-flash 是 thinking 模型（同 deepseek flash 坑）**：max_tokens 太小（如 200）思考链吃光 → content 空。**必须给足 max_tokens（≥4096 稳妥）**；长提示词场景防 content 空参考 8192+
- 8-27 旧指令"仅 deepseek-v4-flash 与 gpt-5.6-sol，禁 v4-pro"已随本次废止（config 注释已更新）

## 二、本轮认知内核成果（全部完成，19/19 蓝图）

| 块 | 状态 | 关键产出 |
|---|---|---|
| 蓝图 #8/#9/#10 资产卡三维 | ✅ | valuation_calc.py + 8问框架 + 资料充分度（71 号，git 0f96d60+61d7316）|
| 检修 7 项 | ✅ | _sane 护栏 / ASSET_STRUCTURE 判据 / snapshot 兜底（61d7316）|
| 占位锚点修复 | ✅ | 1153 假锚定→1040 真实锚定+113 降级（72 号，backfill_placeholder_anchors.py）|
| #18 核对 | ✅ | 已随 #5/#13/#66/#70 落地，无需开发 |
| evidence_tagger 版本漂移 | ✅ | 本地补 anchor>0 + 新增 `--view-ids` 参数 |
| 全量测试 | ✅ | 184 passed（+71 新增）|

## 三、挂起任务（唯一未闭环，等批跑完成）

**252 条 tagger 补标注批跑**（Mac 后台，本地 SSH 长连接托管 proc_38c5a721b7d9）：
- 任务：`evidence_tagger.py --view-ids $(cat /tmp/missing_ids.txt)`（252 条，gpt-5.6-sol，预计 40-120 分钟）
- **完成会自动通知**（notify_on_complete），通知后立即收尾：
  1. `claim_backfill.py`（Mac，确定性免费）补 252 条 claim
  2. 全量校验：占位 0 / anchored 7660+252 / annotation 7484+252 / claim 7484+252
  3. 更新 72 号执行记录结论
- 批跑已备份库：`qianboshi_quality.db.bak_20260901_132834_placeholder_fix`
- **9-01 14:30 事故记录**：13:28 启动的首轮批跑空转 53 分钟零写入。根因=Mac Clash 系统代理劫持 requests→Py3.9 LibreSSL 走代理隧道必 SSLEOFError→脚本 15/30/45s 退避无限循环。已修：evidence_tagger.py 加 `Session(trust_env=False)`（本地 937be1c，已 scp 同步 Mac），冒烟 2/2 写库通过后 14:27 重启批跑（PID 8920，nohup+文件重定向 `/tmp/tagger_batch_0901.log`，脱离管道防 SSH 断开 SIGPIPE）。约 25 秒/条，预计 ~100 分钟完成

## 四、网页更新待办（用户指定新会话做）

- 仓库：`E:\qianboshi-portfolio`（独立 git），部署 https://yuyeccc.github.io/qianboshi-portfolio/
- 待加内容：本轮认知内核（19/19 + 资产卡三维 + "可验证性"叙事）
- 部署坑（skill qianboshi-agent L70-72）：Vite base 用 loadEnv / createHashRouter / snapshot 模式 `--mode snapshot` / 404.html 禁重定向
- **联系区红线（memory）**：只放 GitHub+交流群，邮箱电话永不进 HTML

## 五、环境速查（坑清单）

| 项 | 说明 |
|---|---|
| Python | 一律 `C:/Python314/python.exe` + `env -u PYTHONPATH`（PYTHONPATH 被 hermes venv 劫持）|
| Clash 代理 | 127.0.0.1:7897；LLM 直连 `trust_env=False` 防 Clash 干扰（deepseek 时代坑，GLM 大概率同）|
| Mac SSH | `SSH_ASKPASS="$HOME/askpass.sh" SSH_ASKPASS_REQUIRE=force DISPLAY=dummy` + `-o PubkeyAuthentication=no`；IP 192.168.1.25（漂移则 mDNS 嗅探）；密码四空格 |
| Mac 权威库 | `~/qianboshi_task/data/evidence/qianboshi_quality.db`（本地是空壳！）|
| 跑批铁律 | Mac 多线程 SSL Py3.9 SIGSEGV → multiprocessing；nohup 被 SSH 断杀 → 本地 SSH 长连接托管 |
| GLM thinking | max_tokens 给足 ≥4096，小值 content 空 |
| 模型凭据 | qianboshi 读 .env（ZAI_API_KEY 已就位）；Hermes zai 读 ZAI_API_KEY |

## 六、验证清单（接手后先跑）

```bash
# 1. GLM 通（期望: GLM切换成功）
cd /e/qianboshi-agent && env -u PYTHONPATH C:/Python314/python.exe -c "import sys;sys.path.insert(0,'scripts');from config_loader import get_llm_config,load_config;print(get_llm_config(load_config())['routine_model'])"
# 2. 全量回归（期望 184 passed）
env -u PYTHONPATH C:/Python314/python.exe -m pytest tests/ -q
# 3. 批跑状态（期望: 完成后已接 claim_backfill + 校验）
# 4. git 干净
git status --short
```

## 七、新会话开场指引

> 读 `E:\qianboshi-agent\data\webpage_design\73_交接文档_20260901.md` → 批跑通知若已到，先收尾（claim_backfill+校验，见第三节）→ 网页更新按用户安排 → qianboshi 后续开发按 backlog（72 号执行记录第七节）。
