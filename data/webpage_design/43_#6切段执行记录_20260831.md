# 43_#6 reasoning_unit 语义切段_执行记录_20260831.md

> 生成：老手 | 日期：2026-08-31 | 上游：42_#6切段方案_融合终版（gpt 字段级设计 + 老手自审补洞）
> 状态：✅ v1 落地完成（schema + 全量跑批 + 验证 + 渲染全通）
> 下一刀参考：37 号交接文档队列（#11v2 合规门接入 MCP → #17v2 纪律检查嵌入决策台 → #14v2 真回测）

---

## 一、执行摘要

| 模块 | 产物 | 验证 |
|---|---|---|
| A schema 迁移 | ru_schema_migrate.py（+8 列 +5 索引 +legacy 标记） | 幂等验证 ✅，备份 .bak_20260831_pre_ru |
| B 窗口构建 | ru_window_build.py（三分类 T1/T2/T3） | T1 3757 / T2 2240(1465窗) / T3 1487 |
| C 切段跑批 | ru_tagger.py（确定性 + LLM 4 进程 multiprocessing） | done 1496 窗 零失败，ok units 4341+ |
| D 补漏 | ru_backfill.py（62 条重跑幂等） | 覆盖 7476/7484（99.9%），剩 8 条 LLM 未返回 |
| E 验证 | ru_verify.py（分层抽样 300） | 模板误切 0 / conclusion 缺失 0 / relation 非法 0 / 锚点对齐 100% |
| F 渲染 | export_dimensions v6 + reasoning_chain | reasoning 挂载 7476/7484，论证链渲染实测 ✅ |

## 二、数据结论（2026-08-31 实测）

- **reasoning_unit ru_v1 总量 9467+**：ok 4341（LLM 五元组）+ template_only 3757 + partial_anchor 1487 + 补漏/补 core 若干
- **覆盖 7476/7484（99.9%）**：剩 8 条 LLM 输出未返回（无 unit，非虚构，needs_review 队列）；3 条 LLM 只给 supporting/reason 缺 core_claim → 已补 core_claim unit
- **五元组填充率**（ok 4341）：premise 69.0% / mechanism 42.9% / trigger 26.9% / invalid 5.5%（无则 NULL，gpt 确认符合"原文未给出则 NULL"设计；trigger/invalid 显式条件稀缺属预期）
- **模板句 3757 只给 conclusion（template_only）**：不调 LLM，零误切（0.00%，门槛 <2%）
- **质量门槛**：锚点对齐 100%（≥95% ✅）、模板误切 0（<2% ✅）、relation 非法 0、conclusion 缺失 0
- 旧 1143 条（8-29 试跑）标记 ru_v0_legacy 保留不冲突

## 三、关键设计决策（gpt 设计 + 自审 + 复审吸收）

1. **五元组扩展列**（gpt）：premise/mechanism/conclusion/trigger_condition/invalid_condition 全 TEXT NULL，保留旧 unit_type/primary_role/normalized_proposition 兼容
2. **模板句不调 LLM**（gpt）：词典正则识别（复用 #13 is_template_claim），确定性 conclusion + template_only
3. **无锚点不猜**（老手自审补）：1487 占位锚点 claim → partial_anchor 确定性处理，完整五元组等锚点修复后再补
4. **multiprocessing 替代 threading**（踩坑修复）：Python3.9+LibreSSL 4 线程并发 SSL SIGSEGV → 4 进程独立解释器 + fork 继承
5. **幂等断点**（gpt）：input_hash+schema_version 双键、窗口 status、补漏重跑
6. **#14v2 分层约束**（gpt 复审必修）：按 tuple_status（ok/template_only/partial_anchor）分层统计回测，**禁止混合**
7. **补漏闭环**（gpt 复审确认）：覆盖对账（ru_verify）+ 补漏重跑（ru_backfill）机制已建立；剩 8 条保留 needs_review 不被当作已切段

## 四、文件清单

### Mac（~/qianboshi_task/）
| 文件 | 状态 | 备注 |
|---|---|---|
| ru_schema_migrate.py | 新增 | DDL+索引+legacy（幂等） |
| ru_window_build.py | 新增 | 三分类 + 窗口构建（含全部 claim_id） |
| ru_tagger.py | 新增 | 确定性 + LLM 切段（multiprocessing 4 进程） |
| ru_backfill.py | 新增 | 62 条补漏重跑 |
| ru_verify.py | 新增 | 300 条分层抽样质量报告 |
| export_dimensions.py | v5→v6 | +reasoning 段（旧版备份 export_dimensions_v5.bak.py） |

### Windows（E:\qianboshi-agent\）
| 文件 | 状态 | 备注 |
|---|---|---|
| data/views/dimensions_export.json | 更新 | v6（12.5MB，含 reasoning） |
| scripts/claim_dimensions.py | 修改 | +reasoning 挂载 + reasoning_chain() |
| scripts/brief_renderer.py | 修改 | 详情区 +论证链段 |
| tests/test_evidence_pack_13.py | 修改 | +3 个论证链测试 |

## 五、数据刷新链路（v6 版）

```bash
# 1. Mac 端导出（evidence + 冲突 + reasoning）
ssh a1@192.168.1.25 'cd ~/qianboshi_task && python3 export_dimensions.py && python3 conflict_detector.py'
# 2. 拉回本地
scp a1@192.168.1.25:/tmp/dimensions_export.json /e/qianboshi-agent/data/views/dimensions_export.json
scp a1@192.168.1.25:/tmp/conflicts_export.json /e/qianboshi-agent/data/views/conflicts_export.json
# 3. 渲染层重载
```

## 六、验证清单（交付门证据）

```bash
# 1. 覆盖与质量（期望：覆盖 99.9%、模板误切 0、锚点对齐 100%）
ssh a1@192.168.1.25 'cd ~/qianboshi_task && env -u PYTHONPATH python3 ru_verify.py'
# 2. 导出（期望：reasoning 挂载 7476/7484）
ssh a1@192.168.1.25 'cd ~/qianboshi_task && python3 export_dimensions.py'
# 3. 本地 selfcheck + 回归（期望：dims=7484 + 23 passed）
cd /e/qianboshi-agent && C:/Python314/python.exe scripts/claim_dimensions.py && C:/Python314/python.exe -m pytest tests/ -q
```

## 七、遗留（排队，勿阻塞下一刀）

1. **8 条 LLM 未返回 claim**：保留 needs_review（无 unit 非虚构），可下次补漏重跑再试
2. **五元组精确率/召回率**（gpt 复审）：需分层人工抽验（尤其 high 重要度），验证清单已出 20 条抽验样本
3. **LLM 输出完整性校验**（gpt 复审必修 b）：跑批内自动校验"窗口内全部 claim 都有 core_claim"并自动重试——v1 靠补漏闭环，v2 做进跑批
4. **#14v2 分层约束**（gpt 复审必修 c）：ok/template_only/partial_anchor 分层统计，禁止混合回测（schema_meta 记语义）
5. **占位锚点 1487 修复**：partial_anchor 升级完整五元组的前置
6. **模板句条件补全**（gpt 复审：template_only 不支持条件回测）：high 重要度模板句可选 LLM 条件抽取（v2）

## 八、坑记录（本次新增）

| 坑 | 现象 | 修复 |
|---|---|---|
| threading 并发 SSL 段错误 | 4 线程 LLM 调用 SIGSEGV（exit=139），日志无 traceback | 单线程验证 → 改 multiprocessing 4 进程（独立解释器+SSL，fork 继承数据） |
| nohup 进程被 SSH 断开杀 | 远程 nohup 跑批 SSH 断开后进程死 | 本地 SSH 长连接托管（Hermes background + notify_on_complete） |
| LLM units 不全 | 62 条 claim 漏切（32 窗口） | 覆盖对账（ru_verify）+ 补漏重跑（ru_backfill 幂等） |
| LLM 缺 core_claim | 3 条只有 supporting/reason unit | 补 core_claim unit（conclusion 取已有 unit 最高置信结论） |
| schema_version DEFAULT 误标 | ADD COLUMN DEFAULT 'ru_v1' 把旧 1143 条标成 ru_v1 | UPDATE 按 created_at 改标 ru_v0_legacy |
| heredoc 单引号破坏 | ssh 外层单引号截断 SQL 单引号 | 检查/修复脚本一律文件 scp 执行，不走 heredoc |
| Mac 无 timeout 命令 | 调试想限时运行 | 用 SSH 前台 + terminal timeout 或重定向文件看完整输出 |
