# 50_交接文档_20260831.md — qianboshi 认知内核升级会话交接（抽验闭环 + 存量错配批量修复 + #17v2 方案待拍板）

> 交接人：老手 | 日期：2026-08-31 | 上游：46 号交接文档（v2，8-31 晚）+ 48/49 号执行记录
> 本文档自包含：接手者无需读历史对话即可开工。

---

## 一、本会话完成总览（2026-08-31 下半场）

| 模块 | 状态 | 关键产出 |
|---|---|---|
| 上一会话三刀复核 | ✅ | #13/#6/#11v2 审核链完整（gpt 双审），成果实测真实（34 passed/20红队/dims=7484/门禁0 error） |
| 20 条人工抽验补做 | ✅ | 48 号报告：语义一致率 **80% < 90% 门槛**，3 条张冠李戴 + 1 条 ASR 误转（足球→国族） |
| A：4 条已知错配修复 | ✅ | fix_ru_mismatch.py 重抽，重合率 0.60~1.00 全达标 |
| B：ru_tagger 重合率自检 | ✅ | overlap_ratio（阈值 0.25），<0.25 不插入 → ru_window_v2 needs_review 标记 |
| 存量错配批量修复 | ✅ | 49 号报告：389 真错配 → 383 修复（98.5%），写库 **409 units**，真错配率 10.4%→~3.3% |
| #17v2 纪律检查嵌入决策台 | ⏸ **方案待拍板** | 47 号融合定稿（老手版 + gpt 审核 16 条全吸收），含 P0 回撤口径 bug 修复 |

**核心数据结论**：
- 修复前全量扫描：1270/7559 疑似错配（16.8%），gpt 甄别第一轮 260 + unknown 复判 129 = 389 真错配
- 修复后：疑似 981（13.0%，大头为 partial/同义改写），终验抽样真错配率 ~3.3%（降 2/3）
- Mac 库 reasoning_unit ru_v1 总量不变，ok units 4344，覆盖 7476/7484，模板误切 0

## 二、系统架构（数据流向 v6，同 46 号）

``` 
【Mac a1@192.168.1.25】权威数据层 ~/qianboshi_task/
  data/evidence/qianboshi_quality.db（唯一权威库）
    ├─ view_evidence_annotation (7484) / claim (7484) / reasoning_unit (ru_v1 9467+)
    ├─ claim_reasoning_unit (9585+) / layered_backtest (56) / ru_window_v2（含 needs_review 标记行）
    └─ 备份：.bak_20260831_pre_ru / .bak_20260831_fix_ru_mismatch / .bak_20260831_fix_batch / .bak_20260831_fix_batch2
  export_dimensions.py (v6) → /tmp/dimensions_export.json
  conflict_detector.py (v1.1) → /tmp/conflicts_export.json
      ↓ scp
【Windows E:\qianboshi-agent】渲染层
  data/views/dimensions_export.json（12.5MB，7484 dims）
  scripts/claim_dimensions.py / brief_renderer.py / evidence_gate.py / compliance_gate.py / qianboshi_mcp.py
```

**数据刷新链路 v6**（标注/切段更新后必跑，同 46 号）：
```bash
ssh a1@192.168.1.25 'cd ~/qianboshi_task && python3 export_dimensions.py && python3 conflict_detector.py'
scp a1@192.168.1.25:/tmp/dimensions_export.json /e/qianboshi-agent/data/views/
scp a1@192.168.1.25:/tmp/conflicts_export.json /e/qianboshi-agent/data/views/
```

## 三、文件清单（本会话新增/修改）

### Mac（~/qianboshi_task/ 与 /tmp/）
| 文件 | 作用 | 备注 |
|---|---|---|
| ru_tagger.py | **已加重合率自检**（overlap_ratio + needs_review 标记） | 备份 .bak_20260831_pre_selfcheck |
| /tmp/ru_mismatch_scan.py | 批量甄别（1270 分批 gpt 判定） | 4 进程，输出 /tmp/ru_mismatch_list.json |
| /tmp/ru_unknown_recheck.py | unknown 158 复判（补充 129 真错配） | 输出 /tmp/ru_unknown_extra.json |
| /tmp/ru_mismatch_fix.py | 260 条重抽修复（overlap≥0.25 写库） | 输出 fix_report.json |
| /tmp/ru_mismatch_fix2.py | 129 补充 + 66 语义二次判定 | 输出 fix2_report.json |
| /tmp/final_audit.py | 终验抽样（60 条 gpt 判定） | 真错配率 ~3.3% |
| /tmp/ru_manual_verify.py | 20 条抽验样本全量输出 | 48 号报告数据源 |

### Windows（E:\qianboshi-agent\data\webpage_design\）
| 文件 | 作用 |
|---|---|
| 47_#17v2纪律检查嵌入决策台_方案_融合定稿_20260831.md | **#17v2 方案（待拍板）** |
| 48_ru五元组20条抽验报告_20260831.md | 抽验闭环 + A/B 落地记录 |
| 49_存量错配批量修复_执行记录_20260831.md | 批量修复全流程 |

## 四、下一步队列（新会话开工顺序）

1. **#17v2 纪律检查嵌入决策台**（P1，**方案已就绪待拍板**）：
   - 47 号融合定稿：P0 修回撤口径（盈利组合误报 61.5% bug，改 net_invested 口径 + cost_drawdown 命名 + status=unknown 防除零）+ discipline 段嵌入 decision_desk + agent.py 第 4 段注入 + 模板 v3 纪律契约 + output_gate 硬阻断（**注意：agent.py 简报链路当前完全不过 output_gate，需先接入 brief_post_check**）+ 12 项测试
   - 拍板后执行：改 5 文件（discipline_checker/decision_desk/agent/模板/compliance_gate）+ 1 测试文件，git 可回滚
2. #14v2 真回测（**按 tuple_status ok/template_only/partial_anchor 分层，禁止混合**，需 prediction_events + OOS）
3. 阈值校准（0.70/0.50 待历史命中率校准）
4. 占位锚点 1487 修复（partial_anchor 升级完整五元组前置）
5. 模板句条件补全 v2（high 重要度模板句可选 LLM 条件抽取）
6. 账本治理 P1（portfolio.json history 不完整，缺 159992 买入 989.01 → 建议补流水/position ledger）
7. portfolio_max_cost 规则未实现（配置已存在）
8. date 脏数据 56 条（2016×39 + 2023×17，view_extractor 历史抓错）

## 五、红线（9 条，同 46 号 + 新增）

1. unknown 不猜测，缺失标 `❔原文缺失`/`待验证`
2. 表达类型（fact/interpretation/forecast）≠ 可信度等级，渲染并列 source_layer/verification_status
3. 冲突中心只并列原文，不合并结论
4. 无来源数字不进首屏（evidence_gate，--no-evidence-gate 可关）
5. 模板句确定性处理不调 LLM，五元组留 NULL 不臆造
6. 回测按 tuple_status 分层，禁止混合
7. 只比对不输出调仓（纪律检查）；简报 LLM 链路禁买卖建议（红线 9 + output_gate 兜底）
8. Mac 跑批 DDL 禁并发；qianboshi_quality.db 跑批期间禁外部 DDL
9. **新增**：抽验必须逐条对照原文判定（打印 claim/conclusion 前 N 字不算核验）；LLM 结论串位风险 → ru_tagger 自检 + 人工抽验双保险

## 六、环境手册（同 46 号，速查）

- **Mac SSH 铁律**：`export SSH_ASKPASS="$HOME/askpass.sh" SSH_ASKPASS_REQUIRE=force DISPLAY=dummy` + `-o PubkeyAuthentication=no`；密码四空格
- **跑批铁律**：multiprocessing 勿 threading（Py3.9+LibreSSL SIGSEGV）；nohup 被 SSH 断开杀 → **本地 SSH 长连接托管**（terminal background=true + notify_on_complete）；脚本 scp 执行勿走 heredoc（单引号坑）；`env -u PYTHONPATH python3`
- **LLM 调用**：Mac 用 fluxionai premium stream（config.yaml llm.premium，防 524）；本地 gpt-5.6-sol 用 codex exec 直连（勿走 call_premium）；`</dev/null` 防 stdin 挂
- **本地 Python**：C:/Python314/python.exe；unset PYTHONPATH（被劫持到 hermes-web-ui venv）
- **挂件 todo**：完成步骤即更新 studio-whale todo（revision 214，剩余：#17v2 + 数据库迁移方案）

## 七、坑记录（本会话新增 5 条）

| 坑 | 现象 → 修复 |
|---|---|
| ru_verify 抽验清单是摆设 | 只打印 claim/conclusion 前 N 字，无核验动作 → 补做 20 条对照原文判定（48 号） |
| LLM 结论串位 | 结论取同窗口其他句子（3/20 抽验）→ 重抽 + overlap 自检 + needs_review |
| 词面重合率误拦同义改写 | overlap<0.25 但语义正确（66 条中 63 条）→ gpt 语义二次判定兜底 |
| fix2 B 类空 claim bug | `_mp_judge_existing` 未查库传空 claim_text → 0/66 假象 → 补查库逻辑后 63/66 |
| nohup 后台被杀 | SSH 断开即死 → 本地 SSH 长连接托管（background + notify） |

## 八、验证清单（接手后先跑确认环境完好）

```bash
# 1. Mac 库（期望：annotation 7484 / claim 7484 / ru_v1 9467+ / 覆盖 7476+ / 模板误切 0）
export SSH_ASKPASS="$HOME/askpass.sh" SSH_ASKPASS_REQUIRE=force DISPLAY=dummy
ssh -o PubkeyAuthentication=no a1@192.168.1.25 'cd ~/qianboshi_task && env -u PYTHONPATH python3 ru_verify.py 2>&1 | head -14'
# 2. 本地渲染（期望 dims=7484 + conflicts=465）
cd /e/qianboshi-agent && C:/Python314/python.exe scripts/claim_dimensions.py
# 3. 回归（期望 34 passed）
C:/Python314/python.exe -m pytest tests/ -q
# 4. 合规红队（期望 20/20）
C:/Python314/python.exe scripts/compliance_gate.py --selfcheck
# 5. 门禁（真实简报期望 errors=0）
C:/Python314/python.exe scripts/evidence_gate.py data/briefs/日报_2026-08-28.md
```

五项全过 = 环境完好，直接从第四节第 1 项（#17v2）开工：读 47 号方案 → 拍板 → 执行 → 验证。
